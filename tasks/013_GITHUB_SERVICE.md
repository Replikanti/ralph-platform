# Task 013: Create GitHubService

## Objective
Extract the Octokit-based GitHub PR creation and description generation logic from `agent.ts` into an injectable Ts.ED service.

## Prerequisites
- 003 (env config with `GITHUB_TOKEN`)
- 006 (domain/WorkspaceManager.ts has `parseRepoUrl`)

## Reference Files
- `src/agent.ts` lines 96-167 (`generatePRDescription` function)
- `src/agent.ts` lines 169-188 (`createPullRequest` function)
- `src/workspace.ts` lines 10-16 (`parseRepoUrl` function)

## Deliverables
- `src/services/GitHubService.ts`

## Instructions

```typescript
import { Service } from "@tsed/common";
import { Logger } from "@tsed/logger";
import { parseRepoUrl } from "../domain/WorkspaceManager";
import { GITHUB_TOKEN } from "../config/env";

@Service()
export class GitHubService {
    private logger = new Logger("GitHubService");

    async createPullRequest(
        repoUrl: string,
        branchName: string,
        title: string,
        body: string
    ): Promise<string | null> {
        const { owner, repo } = parseRepoUrl(repoUrl);
        try {
            const { Octokit } = await import("@octokit/rest");
            const octokit = new Octokit({ auth: GITHUB_TOKEN });

            const response = await octokit.rest.pulls.create({
                owner,
                repo,
                title,
                body,
                head: branchName,
                base: "main",
            });
            return response.data.html_url;
        } catch (e: any) {
            this.logger.error(`PR creation failed: ${e.message}`);
            return null;
        }
    }

    async generatePRDescription(
        git: any,
        taskDescription: string,
        validationResult: { success: boolean; output: string; languages?: string[] }
    ): Promise<string> {
        try {
            // Get git diff stats between main and HEAD
            const diffStats = await git.diffSummary(["main"]);
            const filesChanged = diffStats.files?.length || 0;
            const insertions = diffStats.insertions || 0;
            const deletions = diffStats.deletions || 0;

            // Count test files
            const testFiles =
                diffStats.files?.filter(
                    (f: any) =>
                        f.file?.includes(".test.") ||
                        f.file?.includes(".spec.") ||
                        f.file?.includes("tests/") ||
                        f.file?.includes("__tests__/")
                ).length || 0;

            // Parse validation results
            const validationSummary: string[] = [];
            const output = validationResult.output;

            if (output.includes("Biome")) {
                validationSummary.push(
                    output.includes("\u2713") || output.includes("pass") ? "\u2705 Biome" : "\u26a0\ufe0f Biome"
                );
            }
            if (output.includes("TSC") || output.includes("TypeScript")) {
                validationSummary.push(
                    output.includes("\u2713") || output.includes("0 error") ? "\u2705 TSC" : "\u26a0\ufe0f TSC"
                );
            }
            if (output.includes("Ruff")) {
                validationSummary.push(
                    output.includes("\u2713") || output.includes("pass") ? "\u2705 Ruff" : "\u26a0\ufe0f Ruff"
                );
            }
            if (output.includes("Mypy")) {
                validationSummary.push(
                    output.includes("\u2713") || output.includes("Success") ? "\u2705 Mypy" : "\u26a0\ufe0f Mypy"
                );
            }
            if (output.includes("Trivy")) {
                validationSummary.push(
                    output.includes("\u2713") || output.includes("0 vulnerabilities")
                        ? "\u2705 Trivy"
                        : "\u26a0\ufe0f Trivy"
                );
            }

            const sections: string[] = [];

            if (taskDescription && taskDescription.trim()) {
                sections.push(`## Task Description\n\n${taskDescription.trim()}`);
            }

            sections.push("## Changes Summary\n");
            sections.push(`- **Files changed:** ${filesChanged}`);
            sections.push(`- **Insertions:** +${insertions}`);
            sections.push(`- **Deletions:** -${deletions}`);
            if (testFiles > 0) {
                sections.push(`- **Test files:** ${testFiles} modified/added`);
            }

            if (validationSummary.length > 0) {
                sections.push("\n## Validation\n");
                sections.push(validationSummary.join(" \u2022 "));
            }

            sections.push(
                "\n---\nGenerated by [Ralph Platform](https://github.com/Replikanti/ralph-platform)"
            );

            return sections.join("\n");
        } catch (error) {
            this.logger.warn("Failed to generate PR description:", error);
            return taskDescription || "Implementation completed by Ralph";
        }
    }
}
```

### Key Design Decisions

1. **Dynamic Octokit import**: Uses `await import("@octokit/rest")` (lazy load) to avoid importing Octokit when GitHub features aren't needed. This matches the current pattern in agent.ts.
2. **`parseRepoUrl` from domain**: Uses the domain function for URL parsing.
3. **PR description generation**: Extracted from agent.ts `generatePRDescription` function. Logic is identical.
4. **`git` parameter**: The SimpleGit instance is passed in (not injected) because it's workspace-specific (created per job).

## Acceptance Criteria
- [ ] `src/services/GitHubService.ts` exists with `@Service()` decorator
- [ ] `createPullRequest` method with same logic as agent.ts
- [ ] `generatePRDescription` method with same diff stats and validation parsing
- [ ] Uses `parseRepoUrl` from `domain/WorkspaceManager.ts`
- [ ] Uses `GITHUB_TOKEN` from `config/env.ts`
- [ ] Dynamic Octokit import preserved
- [ ] `npm run build` compiles without errors
