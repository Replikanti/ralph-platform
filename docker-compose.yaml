services:
  redis:
    image: redis:alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  ralph:
    build: .
    command: npm start
    ports:
      - "3000:3000"
    environment:
      - REDIS_URL=redis://redis:6379
      - PORT=3000
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - /tmp/ralph-workspaces:/tmp/ralph-workspaces

volumes:
  redis_data:
